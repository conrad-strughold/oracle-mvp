language: node_js
node_js:
  - '8'
install:
- npm install -g truffle
- npm install -g ganache-cli
- npm install
script:
  - npm test

after_success:
  - cat coverage/lcov.info | ./node_modules/.bin/coveralls
